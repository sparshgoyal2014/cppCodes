#include <iostream>
using namespace std;
int main()
{
    int x, y, z, sum;
    float avg;
    cout << "enter the no's." << endl;
    cin >> x >> y >> z;
    sum = x + y + z;
    cout << "the sum of the avobe no.'s is " << sum << endl;
    avg = sum / 3 cout << "the average of the avobe numbers is " << avg << endl;
    return 0;
}