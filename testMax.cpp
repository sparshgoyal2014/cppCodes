#include<iostream>
#include<algorithm>
using namespace std;

int main() 
{ 
    // Comparing ASCII values of a and b 
    cout << max(10,20) << "\n"; 
  
    // Returns the first one if both the numbers 
    // are same 
    cout << max(7,7); 
  
return 0; 
}  