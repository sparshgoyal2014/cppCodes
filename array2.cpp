#include <iostream>
using namespace std;
int main()
{
    int arr[50] = {1, 2, 3, 4, 5};// remaining will be zero by default (I think only in this case i.e. initializing at the time of declaration with definitive size)
    cout << arr << endl;
    return 0;
}